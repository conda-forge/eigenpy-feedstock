diff --git a/CMakeLists.txt b/CMakeLists.txt
index 92be7fa..1957c87 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -212,6 +212,10 @@ MODERNIZE_TARGET_LINK_LIBRARIES(${PROJECT_NAME} SCOPE PUBLIC
   INCLUDE_DIRS ${NUMPY_INCLUDE_DIR})
 TARGET_INCLUDE_DIRECTORIES(${PROJECT_NAME} SYSTEM PRIVATE ${PYTHON_INCLUDE_DIRS})
 
+include(CMakePrintHelpers)
+cmake_print_variables(NUMPY_INCLUDE_DIR)
+cmake_print_properties(TARGETS Python3::NumPy PROPERTIES
+                       LOCATION INTERFACE_INCLUDE_DIRECTORIES)
 IF(SUFFIX_SO_VERSION)
   SET_TARGET_PROPERTIES(${PROJECT_NAME} PROPERTIES SOVERSION ${PROJECT_VERSION})
 ENDIF(SUFFIX_SO_VERSION)
